# 02.1 - LÃ³gica Proposicional Booleana (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/01_logica_booleana/` (`01_logica_booleana.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**CÃ³digos usados:** `codes/` (veja a [seÃ§Ã£o 9](#9-cÃ³digos-fonte-usados-no-resumo))
**Aula:** 01 do mÃ³dulo ProgramaÃ§Ã£o LÃ³gica

---

## 1. Contexto histÃ³rico

- **George Boole** (1815â€“1864, Inglaterra): propÃ´s a formalizaÃ§Ã£o da lÃ³gica por meio da matemÃ¡tica.
  - *The Mathematical Analysis of Logic* (1847) â€” introduz os fundamentos da **lÃ³gica proposicional booleana**.
  - *An Investigation of the Laws of Thought* (1849) â€” resgata e expande esses fundamentos.
- **Prolog (1972):** contraÃ§Ã£o de *"PROgramming in LOGic"*.
  - Proponentes: **Alain Colmerauer** e **Philippe Roussel**.
  - InspiraÃ§Ã£o: **Robert Kowalski**.
  - Tem raÃ­zes na lÃ³gica de primeira ordem.

---

## 2. Termos primitivos e axiomas

### O problema da regressÃ£o infinita de definiÃ§Ãµes

Toda definiÃ§Ã£o usa palavras, e cada palavra usada na definiÃ§Ã£o precisaria, por sua vez, ser
definida. Exemplo â€” "conjunÃ§Ã£o":

> *"a âˆ§ b Ã© verdadeiro quando a e b sÃ£o ambos verdadeiros"*

- O que Ã© "verdadeiro"? â†’ precisaria de outra definiÃ§Ã£o.
- O que Ã© "proposiÃ§Ã£o"? â†’ de outra...
- O que Ã© "afirmaÃ§Ã£o", "sentenÃ§a", "significado"? â†’ e assim por diante.

Cada definiÃ§Ã£o **ramifica** em novas definiÃ§Ãµes, gerando um ciclo infinito: ou voltamos
sempre ao mesmo ponto (definiÃ§Ãµes circulares), ou nunca chegamos ao fim
(*regressÃ£o infinita*). Formalizar a lÃ³gica exige **parar o ciclo em algum lugar** â€”
um "limite" onde o vocabulÃ¡rio e as regras sÃ£o **aceitos sem definiÃ§Ã£o nem prova**.

### Termos primitivos â€” o vocabulÃ¡rio aceito sem definiÃ§Ã£o - Base da programaÃ§Ã£o lÃ³gica

SÃ£o os **nomes fundamentais** do sistema: nÃ£o sÃ£o definidos, sÃ£o *dados*. Qualquer outra
definiÃ§Ã£o Ã© construÃ­da em cima deles.

| Termo primitivo | Papel no sistema |
|---|---|
| **ProposiÃ§Ã£o** | A unidade mÃ­nima que pode ser julgada: algo que afirma um fato e pode ser avaliado como verdadeiro ou falso ("estÃ¡ chovendo", "2 + 2 = 4"). |
| **Verdadeiro** | Um dos dois valores lÃ³gicos possÃ­veis de uma proposiÃ§Ã£o. |
| **Falso** | O outro valor lÃ³gico possÃ­vel. |

Ex.: a proposiÃ§Ã£o "2 + 2 = 4" **Ã©** verdadeira â€” nÃ£o precisamos definir o que Ã©
"verdadeiro" para usÃ¡-lo; tomamos o termo como ponto de partida.

### Axiomas â€” as regras aceitas sem prova

SÃ£o **proposiÃ§Ãµes que nÃ£o se provam**: sÃ£o assumidas como verdadeiras para servir de
alicerce. Toda deduÃ§Ã£o do sistema parte delas; provar um axioma exigiria outros axiomas
â€” e cairÃ­amos de novo no ciclo.

| Axioma | Enunciado |
|---|---|
| **PrincÃ­pio do terceiro excluÃ­do** | Toda proposiÃ§Ã£o Ã© verdadeira **ou** falsa â€” nÃ£o existe uma terceira possibilidade (*tertium non datur*). |
| **PrincÃ­pio da nÃ£o-contradiÃ§Ã£o** | Uma proposiÃ§Ã£o **nÃ£o pode ser** verdadeira e falsa ao mesmo tempo. |

Juntos, os dois axiomas garantem que o universo de valores lÃ³gicos Ã© exatamente
{verdadeiro, falso}: toda proposiÃ§Ã£o recebe exatamente um dos dois valores â€” pelo menos um
(terceiro excluÃ­do) e no mÃ¡ximo um (nÃ£o-contradiÃ§Ã£o). Ã‰ esse pressuposto que torna a lÃ³gica
booleana possÃ­vel (e que, mais adiante, o Prolog explora com `true/0` e `false/0`).

### Analogia: a geometria de Euclides

O mesmo padrÃ£o aparece na geometria clÃ¡ssica:

- **Primitivos:** ponto, reta e plano â€” nÃ£o tÃªm definiÃ§Ã£o formal, sÃ£o intuitivos.
- **Axiomas (postulados):** "por dois pontos distintos passa uma Ãºnica reta" â€” aceitos
  sem prova.
- **Derivados:** teoremas como o de PitÃ¡goras, **provados** a partir dos primitivos e axiomas.

Na lÃ³gica proposicional a estrutura Ã© anÃ¡loga: primitivos + axiomas sÃ£o o "tÃ©rreo" do
edifÃ­cio; conectivos, fatos, regras e teoremas sÃ£o os andares construÃ­dos em cima.

### Resumo

| Termos primitivos (vocabulÃ¡rio dado) | Axiomas (verdades assumidas) |
|---|---|
| ProposiÃ§Ã£o | PrincÃ­pio do **terceiro excluÃ­do** |
| Verdadeiro | PrincÃ­pio da **nÃ£o-contradiÃ§Ã£o** |
| Falso | |

---

## 3. Conectivos da lÃ³gica proposicional booleana

> Conectivos tÃªm **vÃ¡rias notaÃ§Ãµes** conforme a Ã¡rea (matemÃ¡tica, programaÃ§Ã£o, circuitos).
> Abaixo, a tabela com os sÃ­mbolos mais comuns de cada versÃ£o.

| OperaÃ§Ã£o | SÃ­mbolos (versÃµes) | Leitura | DefiniÃ§Ã£o |
|---|---|---|---|
| **NegaÃ§Ã£o** | $\lnot a$ Â· $\sim a$ Â· $\bar{a}$ Â· `!a` Â· `NOT a` | nÃ£o $a$ | Inverte o valor lÃ³gico de $a$ |
| **DisjunÃ§Ã£o (OR)** | $a \vee b$ Â· $a \mid b$ Â· $a + b$ Â· `a \|\| b` Â· `a or b` | $a$ ou $b$ | Falso apenas se $a$ e $b$ sÃ£o ambos falsos |
| **ConjunÃ§Ã£o (AND)** | $a \land b$ Â· $a \& b$ Â· $a \cdot b$ Â· `a && b` Â· `a and b` | $a$ e $b$ | Verdadeiro apenas se $a$ e $b$ sÃ£o ambos verdadeiros |
| **Condicional (â†’)** | $a \to b$ Â· $a \Rightarrow b$ Â· $a \supset b$ | se $a$, entÃ£o $b$ | Falso apenas se $a$ Ã© verdadeiro e $b$ Ã© falso |
| **Bicondicional (â†”)** | $a \leftrightarrow b$ Â· $a \iff b$ Â· $a \equiv b$ Â· `a == b` | $a$ se, e somente se, $b$ | Verdadeiro se ambos tÃªm o mesmo valor lÃ³gico |

### Notas sobre as versÃµes

- **NegaÃ§Ã£o:** $\bar{a}$ (barra sobre a letra) Ã© comum em Ã¡lgebra booleana e circuitos;
  `!a` em C/Java; `NOT a` em SQL/Assembly; em Prolog Ã© o predicado `\+/1`.
- **OR/AND:** $+$ e $\cdot$ vÃªm da Ã¡lgebra booleana (de Boole); `||`/`&&` em C/Java;
  `or`/`and` em Python; `\/`/`,` sÃ£o os operadores de controle do Prolog (seÃ§Ã£o 6).
- **Condicional:** $\Rightarrow$ e $\supset$ aparecem em textos de lÃ³gica; o "se...entÃ£o"
  Ã© o mesmo conectivo.
- **Bicondicional:** $\equiv$ Ã© o sÃ­mbolo de equivalÃªncia lÃ³gica; `==` em linguagens
  imperativas compara valores (comportamento anÃ¡logo ao "mesmo valor lÃ³gico").

### Em Python (para comparar)

```python
a = True; b = False
not a          # False          (negaÃ§Ã£o)
a or b         # True           (disjunÃ§Ã£o)
a and b        # False          (conjunÃ§Ã£o)
a == b         # False          (bicondicional sobre valores)
# Python nÃ£o tem operador direto para a â†’ b (condicional):
# implementa-se como: (not a) or b
```

---

## 4. Valores lÃ³gicos em Prolog -PROgramming in LOGic

- Verdadeiro e falso sÃ£o implementados pelos predicados `true/0` e `false/0`.

```prolog
?- true.
true.

?- false.
false.
```

- **Prolog distingue maiÃºsculas de minÃºsculas**:

```prolog
?- True.
% ... 1,000,000 ............ 10,000,000 years later
%       >> 42 << (last release gives the question)
```

(`True` Ã© uma variÃ¡vel; `true` Ã© o predicado.)

---

## 5. Fatos

- Fatos sÃ£o os predicados mais simples do Prolog e correspondem a **proposiÃ§Ãµes verdadeiras**.
- Sintaxe: `pred(arg1, arg2, ..., argN).`
  - `pred` = **nome** do predicado
  - `arg1..argN` = **argumentos**
  - `N` = **aridade** (nÃºmero de argumentos)
  - `.` = **terminador** (ponto final obrigatÃ³rio)

**Conectivos como fatos** (proposiÃ§Ãµes nÃ£o declaradas sÃ£o consideradas falsas â€” *closed world*):

```prolog
and(true, true).

or(true, true).
or(true, false).
or(false, true).

not(false).
```

### Declarando e consultando

- Fatos vivem em arquivos `.pl`, lidos pelo interpretador.
- `$ prolog -s source.pl` â€” carrega na inicializaÃ§Ã£o.
- `consult/1` e `reconsult/1` â€” carregar/recarregar em sessÃ£o interativa.
- **Programas = consultas** na base de fatos (resposta `true`/`false`):

```prolog
?- consult('codes/conectivos.pl').
true.

?- and(true, false).
false.

?- or(false, true).
true.

?- not(true).
false.
```

---

## 6. ProposiÃ§Ãµes compostas

### Predicado nÃ£o Ã© funÃ§Ã£o â€” nunca retorna nada!

Antes de tudo, um ponto central: **predicado â‰  funÃ§Ã£o**. Em Prolog, um predicado **nunca
retorna um valor**. Uma funÃ§Ã£o (C, Python, etc.) *computa* e *devolve* um resultado que
pode ser usado como dado de outra expressÃ£o. Um predicado apenas **Ã© provado verdadeiro
ou falso** pelo interpretador â€” a resposta `true`/`false` Ã© o *veredito* da prova, nÃ£o um
valor que volta para ser combinado.

Ã‰ por isso que, se vocÃª tenta usar `and`/`or`/`not` como se fossem funÃ§Ãµes aninhadas:

```prolog
?- and(or(true, false), true).
false.        % falha por unificaÃ§Ã£o, apesar de (F âˆ¨ V) âˆ§ V ser verdadeira
```

...o Prolog **nÃ£o avalia** `or(true, false)` para depois passar o resultado a `and/2`.
Ele tenta **unificar** a consulta `and(or(true, false), true)` contra os fatos da base â€”
e nenhum fato tem como primeiro argumento o termo composto `or(true, false)` (que nem Ã©
um valor lÃ³gico: Ã© um termo). NÃ£o existe "resultado de `or(...)`" para combinar porque
**`or/2` nunca retornou nada** â€” predicado nÃ£o retorna, apenas prova ou falha.

**Em resumo:** proposiÃ§Ãµes compostas nÃ£o funcionam com fatos porque
1. predicado nÃ£o Ã© funÃ§Ã£o (nÃ£o hÃ¡ valor de retorno para aninhar);
2. a unificaÃ§Ã£o compara a consulta inteira, termo por termo, com os fatos;
3. um termo composto como `or(true, false)` nÃ£o casa com nenhum fato declarado.

A linguagem resolve isso com **predicados de controle** â€” que sÃ£o traduzidos pelo
compilador (seÃ§Ã£o abaixo), nÃ£o provados como fatos:

```prolog
?- (false ; true), true.
true.
```

### Predicados de controle

SÃ£o predicados que implementam estruturas de controle e, em geral, sÃ£o traduzidos pelo
compilador (operadores infixados):

| Predicado | OperaÃ§Ã£o | ObservaÃ§Ã£o |
|---|---|---|
| `,/2` | conjunÃ§Ã£o (e) | |
| `;/2` | disjunÃ§Ã£o (ou) | usar parÃªnteses Ã© fortemente recomendado |
| `\+/1` | negaÃ§Ã£o | verdadeiro apenas quando nÃ£o hÃ¡ evidÃªncia/prova do argumento (negaÃ§Ã£o por falha) |
| `true/0`, `false/0` | constantes de controle | |

```prolog
?- (false ; true), true.
true.
```

### Ordem de precedÃªncia dos operadores

Todo operador Prolog tem uma **precedÃªncia** (inteiro entre 0 e 1200) e um **tipo** de
associatividade. Regra de ouro: **quanto menor o nÃºmero, maior a precedÃªncia** â€” o
operador de menor nÃºmero "liga mais forte" e agrupa seus operandos antes.

| PrecedÃªncia | Operador | Tipo | Significado |
|---|---|---|---|
| 1200 | `:-` Â· `-->` | xfx | regra / implicaÃ§Ã£o |
| 1100 | `;` | xfy | **disjunÃ§Ã£o** (ou) |
| 1050 | `->` | xfy | if-then |
| 1000 | `,` | xfy | **conjunÃ§Ã£o** (e) |
| 900 | `\+` | fy | **negaÃ§Ã£o** (nÃ£o) |
| 700 | `=` `==` `\==` `<` `>` `=<` `>=` `=:=` `=\=` `is` | xfx | unificaÃ§Ã£o, comparaÃ§Ã£o, avaliaÃ§Ã£o |
| 500 | `+` `-` | yfx | soma, subtraÃ§Ã£o |
| 400 | `*` `/` `//` `mod` | yfx | multiplicaÃ§Ã£o, divisÃ£o, resto |
| 200 | `^` | xfy | potÃªncia (associa Ã  direita) |
| 200 | `-` (unÃ¡rio) `+` (unÃ¡rio) | fy | sinal |
| 0 | â€” | â€” | **apaga** a declaraÃ§Ã£o do operador |

**Associatividade** (tipos com `f` = posiÃ§Ã£o do operador, `y` = lado que aceita operador
da mesma precedÃªncia):

- `xfx` â€” nÃ£o associativo (`=` em `a = b = c` Ã© erro)
- `xfy` â€” associativo **Ã  direita**
- `yfx` â€” associativo **Ã  esquerda** (aritmÃ©tica usual: `a - b - c` = `(a - b) - c`)
- `fy` â€” prefixado (`\+`); `fx` â€” prefixado; `xf` â€” pÃ³s-fixado

**Por que usar parÃªnteses com `;`:** como a conjunÃ§Ã£o (1000) tem **maior precedÃªncia**
que a disjunÃ§Ã£o (1100), `a ; b , c` Ã© lido como `a ; (b , c)`, e nÃ£o como
`(a ; b) , c`. Para expressar `(F âˆ¨ V) âˆ§ V` em Prolog, os parÃªnteses em
`(false ; true), true` sÃ£o obrigatÃ³rios â€” sem eles a leitura seria
`false ; (true, true)`, outra proposiÃ§Ã£o.

**Declarando e consultando operadores:**

```prolog
:- op(Precedence, Type, Name).   % declara (ou redefine) um operador
?- current_op(Precedence, Type, Name).   % consulta operadores definidos
```

---

## 8. BÃ´nus: `True` e `False` em Python sÃ£o objetos â€” o que isso significa?

### A ideia por trÃ¡s

Em Python, **tudo Ã© objeto** â€” inclusive valores lÃ³gicos. Quando vocÃª escreve `True` ou
`False`, estÃ¡ se referindo a **dois objetos concretos que existem na memÃ³ria**, cada um
com um tipo, uma identidade e atÃ© mÃ©todos. Eles nÃ£o sÃ£o apenas "palavras da linguagem"
(sintaxe), sÃ£o **dados** que podem ser guardados, passados e manipulados como qualquer
outro valor.

`True` e `False` sÃ£o as **Ãºnicas duas instÃ¢ncias** da classe `bool` (a classe `bool` Ã© um
*singleton* duplo: nÃ£o existe um terceiro booleano). AlÃ©m disso, `bool` Ã© **subclasse de
`int`** â€” heranÃ§a que dÃ¡ comportamento inesperado a quem vem de outras linguagens.

### O que ser objeto permite fazer

```python
type(True)             # <class 'bool'>  -> tem um tipo
isinstance(True, int)  # True            -> bool herda de int
True + True            # 2               -> heranÃ§a de int em aÃ§Ã£o
True.bit_length()      # 1               -> tem mÃ©todos

x = True
y = True
x is y                 # True  -> mesmo objeto na memÃ³ria (singleton)
x is True              # True  -> identidade de objeto
True == 1              # True  -> igual em valor...
True is 1              # False -> ...mas objetos diferentes!

valores = [True, False, True]      # pode ir em listas
d = {True: "sim", False: "nÃ£o"}    # pode ser chave de dicionÃ¡rio
def f(a, b): return a and b        # pode ser argumento/retorno de funÃ§Ã£o
```

### O contraste com Prolog (o porquÃª da pergunta)

Em Prolog, `true` e `false` **nÃ£o sÃ£o objetos nem dados** â€” sÃ£o **predicados** (`true/0`,
`false/0`): *aÃ§Ãµes* que vocÃª pergunta ao interpretador. Eles sÃ³ existem no momento de uma
**consulta**, como resultado de derivaÃ§Ã£o/unificaÃ§Ã£o. NÃ£o dÃ¡ para "guardar" `true` numa
variÃ¡vel ou passÃ¡-lo como dado de um programa Prolog â€” ele Ã© o resultado de uma execuÃ§Ã£o,
nÃ£o um valor manipulÃ¡vel.

| Aspecto | Python (`True`/`False`) | Prolog (`true`/`false`) |
|---|---|---|
| Natureza | **Objetos** (dados) â€” instÃ¢ncias de `bool` | **Predicados** â€” `true/0`, `false/0` |
| O que sÃ£o | Valores que existem na memÃ³ria | Resultados de consultas ao interpretador |
| Podem ser armazenados? | Sim (variÃ¡vel, lista, dict, chave) | NÃ£o â€” sÃ£o respostas, nÃ£o dados |
| TÃªm tipo/identidade? | Sim (`bool`, singleton, herda de `int`) | NÃ£o â€” apenas nome+aridade |
| Papel | *Citizen of first class* (dado manipulÃ¡vel) | VerificaÃ§Ã£o dentro de uma derivaÃ§Ã£o |
| Caso de uso tÃ­pico | `if x:` em expressÃµes e estruturas | `?- true.` como alvo de consulta |

**Resumo:** dizer que "True/False em Python sÃ£o objetos" significa que, naquele
paradigma (imperativo/POO), o valor lÃ³gico Ã© tratado como **dado de primeira classe** â€”
algo que existe, tem tipo, e pode circular pelo programa. No Prolog (paradigma lÃ³gico),
verdadeiro/falso nÃ£o sÃ£o coisas que circulam: sÃ£o o **veredito** que o interpretador
emite ao tentar provar uma consulta.

---

## 7. ReferÃªncias da aula

- SWI-Prolog: https://www.swi-prolog.org/
- Wolfram, Stephen. *George Boole: A 200-Year View*.
- dtonhofer/prolog_notes. *Negation as Failure*.
- Merrit, Dennis. *Adventure in Prolog*, Amzi!, 191 pgs, 2017.

---

## 9. CÃ³digos fonte usados no resumo

CÃ³pias dos arquivos de `codes/` da aula 01, salvos em `codes/` desta pasta:

| Arquivo | ConteÃºdo | Usado na seÃ§Ã£o |
|---|---|---|
| [`true_false.pl`](codes/true_false.pl) | `true.` / `false.` | 4 (valores lÃ³gicos) |
| [`42.pl`](codes/42.pl) | consulta `True` (maiÃºscula) | 4 (case-sensitive) |
| [`conectivos.pl`](codes/conectivos.pl) | conectivos como fatos | 5 (fatos) |
| [`program.pl`](codes/program.pl) | sessÃ£o interativa (`consult/1`) | 5 (declarando e consultando) |
| [`composta_fail.pl`](codes/composta_fail.pl) | proposiÃ§Ã£o composta que falha | 6 (predicado â‰  funÃ§Ã£o) |
| [`composta_ok.pl`](codes/composta_ok.pl) | proposiÃ§Ã£o composta com `;`/`,` | 6 (predicados de controle) |

Fonte original: `documento-base/Paradigmas/Programacao_Logica/01_logica_booleana/codes/`.

**Para executar com o SWI-Prolog (Windows):**

```powershell
swipl -s codes/conectivos.pl
```
