# 02.5 - Recursão (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/05_recursao/` (`05_recursao.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**Data:** 27/08/2026
**Códigos usados:** `codes/` (veja a seção 9)
**Aula:** 05 do módulo Programação Lógica

---

## 1. Contexto / Motivação

Prolog não tem laços `for`/`while` como linguagens imperativas. Como escrever algoritmos que
repetem? A resposta é a **recursão** — e em Prolog ela é ainda mais natural, porque a
própria unificação e o backtracking conduzem a recursão. Nesta aula você vê como definir
predicados recursivos, a diferença entre recursão **comum** e **de cauda**, e como usar
**estruturas de dados** (termos compostos) para modelar informações.

---

## 2. Predicados recursivos

Um predicado é definido **recursivamente** se uma ou mais regras de sua definição se
referem **ao próprio predicado**. Toda definição recursiva tem **duas partes**:

1. **Cláusulas-base** — fatos ou generalizações (encerram a recursão).
2. **Regras recursivas** — chamam o próprio predicado (reduzem o problema).

O tratamento das regras recursivas se dá **naturalmente pelo backtracking**.

```prolog
fact(N, F) :-
    N > 0,
    succ(NewN, N),
    fact(NewN, F1),
    F is F1*N.

fact(0, F) :- F is 1.
```

### Características da recursão em Prolog

- **Cada nível de recursão** tem seu próprio conjunto de variáveis.
- A **unificação** entre o objetivo e o padrão de uma regra recursiva define as relações
  entre as variáveis de diferentes níveis.
- É preciso garantir que os **parâmetros não caracterizem uma cláusula-base**. Sem esse
  cuidado, um retorno pela porta `redo` em alguma subconsulta pode fazer um conjunto de
  valores que caracterizava uma cláusula-base **avançar na regra recursiva**, causando
  erros como **laços infinitos**.

---

## 3. Exemplos de recursão

### Fibonacci (iterativo via acumuladores — recursão de cauda)

```prolog
% cláusulas em codes/fibo.pl e codes/fibo2.pl; para testar:
% salve sem as linhas ?- e ?- consult('fibo.pl'). (ou use fibo2.pl, já carregável)
fibTR(N, B, A, F) :-
    N > 0,
    succ(NewN, N),
    C is A + B,
    fibTR(NewN, C, B, F).

fibTR(0, _, Acc, Acc).

:- use_module(library(arithmetic)).
:- arithmetic_function(fib/1).

fib(N, F) :- fibTR(N, 1, 0, F).

?- X is fib(200).
X = 280571172992510140037611932413038677189525.
```

### Recursão simples × recursão de cauda

Considere o fatorial. A versão **não-cauda** (`fact/2`) faz o cálculo **depois** da chamada
recursiva (`F is F1*N`), acumulando chamadas pendentes na pilha. A versão **de cauda**
(`factTR/3`) passa o resultado acumulado (`NewAcc`) para a próxima chamada:

```prolog
% recursão comum
fact(N, F) :- N > 0, succ(NewN, N), fact(NewN, F1), F is F1*N.
fact(0, F) :- F is 1.

% recursão de cauda (otimizada)
factTR(N, Acc, F) :-
    N > 0,
    succ(NewN, N),
    NewAcc is Acc * N,
    factTR(NewN, NewAcc, F).
factTR(0, Acc, F) :- F is Acc.

factorial(N, F) :- factTR(N, 1, F).
```

**Diferença-chave:**

| Recursão comum | Recursão de cauda |
|---|---|
| O resultado depende da chamada recursiva (`F is F1*N`) | O resultado já vem acumulado na chamada |
| Acumula chamadas pendentes na pilha | Pode ser otimizada (a pilha não cresce) |
| Sufoca a pilha em entradas grandes | Suporta entradas muito grandes |

**Passo a passo: `fact(3, F)` × `factTR(3, 1, F)`**

*Recursão comum (`fact/2`)* — o cálculo acontece **na volta** da pilha:

```prolog
fact(3, F):
    succ(2, 3), fact(2, F1), F is F1 * 3      % desce ate a base...
        succ(1, 2), fact(1, F1), F is F1 * 2
            succ(0, 1), fact(0, F1), F is F1 * 1
                fact(0, F) :- F is 1          % base
            F is 1 * 1 = 1                    % ...e calcula subindo
        F is 1 * 2 = 2
    F is 2 * 3 = 6

?- fact(3, F).
F = 6.
```

*Recursão de cauda (`factTR/3`)* — o acumulador **desce pronto**, sem cálculo na volta:

```prolog
factTR(3, 1, F):
    NewAcc is 1 * 3 = 3  ->  factTR(2, 3, F)
    NewAcc is 3 * 2 = 6  ->  factTR(1, 6, F)
    NewAcc is 6 * 1 = 6  ->  factTR(0, 6, F)
                             factTR(0, Acc, F) :- F is Acc.   % base
                             F = 6

?- factTR(3, 1, F).
F = 6.
```

> Na `fact/2` os `is` ficam **pendentes** até a base (a pilha cresce). Na `factTR/3` cada passo
> já entrega o valor acumulado para o próximo (pilha constante).

> Comparando com Python (iterativo): o mesmo Fibonacci iterativo leva ~9,25s para $10^6$;
> em Prolog (recursão de cauda), ~6,49s.

### Exemplos simples para visualizar — recursão + backtracking

Base pequena (salve em `familia.pl` e `?- consult('familia.pl').`):

```prolog
genitor(ana, bruno).
genitor(bruno, carla).
genitor(carla, davi).

ancestral(X, Y) :- genitor(X, Y).                    % caso base
ancestral(X, Y) :- genitor(X, Z), ancestral(Z, Y).   % caso recursivo
```

**Sem `;` → só o 1º ancestral:**

```prolog
?- ancestral(ana, Y).
Y = bruno.
```

**Com `;` → a recursão continua gerando:**

```prolog
?- ancestral(ana, Y).
Y = bruno ;      % genitor(ana, bruno)
Y = carla ;      % ancestral(bruno, Y) -> genitor(bruno, carla)
Y = davi.        % ancestral(carla, Y) -> genitor(carla, davi)
```

**Passo a passo (o que o Prolog encaixa, na ordem):**

```prolog
ancestral(ana, Y):
  genitor(ana, Y)?                Y = bruno            ✓ (1ª cláusula)
  genitor(ana, Z), ancestral(Z, Y)?
      Z = bruno -> ancestral(bruno, Y)
          genitor(bruno, Y)?      Y = carla            ✓
          genitor(bruno, Z2), ancestral(Z2, Y)?
              Z2 = carla -> ancestral(carla, Y)
                  genitor(carla, Y)?  Y = davi         ✓
```

> Sem `;`, para no 1º (`bruno`); com `;`, o `redo` desfaz e **desce a recursão** — igual ao
> `cor/1` da aula 03, só que aqui a busca é recursiva.

---

## 4. Estruturas de dados em Prolog

Uma **estrutura de dados** combina termos primitivos (átomos, inteiros) e estruturas em
**tipos compostos**. A sintaxe de declaração é idêntica a um fato:

```prolog
functor(arg1, arg2, ..., argN).
```

- Cada argumento pode ser um tipo primitivo ou **outra estrutura** (aninhamento).
- A declaração de uma estrutura é **semelhante à declaração de um fato**.

```prolog
car(peugeot, black, 2).
car(honda, red, 4).
```

### Consultas envolvendo estruturas

- A **ordem dos argumentos** influencia o resultado de uma consulta.

```prolog
car(X, red, _).        % que carro é vermelho com 4 portas?
```

- Campos podem ser ignorados com a variável anônima `_`.
- Estruturas podem ser **aninhadas** para legibilidade:

```prolog
car(honda, color(red), doors(4)).
```

> As cláusulas `car(honda, red, 4)` e `car(honda, color(red), doors(4))` **não unificam**:
> os argumentos são estruturas diferentes (`red` ≠ `color(red)`).

### Exemplo — matrioskas (estrutura + recursão)

```prolog
matrioska(a, b).
matrioska(a, c).
matrioska(b, d).
matrioska(b, c).
matrioska(b, f).
matrioska(c, e).
matrioska(c, g).
matrioska(d, g).
matrioska(e, f).
matrioska(f, g).
matrioska(f, h).

% Função recursiva auxiliar: imprime as matrioskas aninhadas
list_(M, L) :-
    nl,
    tab(L),
    print(->),
    tab(1),
    print(M),
    matrioska(M, X),
    NewL is L + 4,
    list_(X, NewL).

list(M, L) :- list_(M, L).
list(_, _).

doll(X) :- matrioska(X, _).
doll(X) :- matrioska(_, X).
```

---

## 5. Referências

- SWI-Prolog: https://www.swi-prolog.org/
- Wikipédia. *Prolog*, acesso em 13/03/2026.
- Merrit, Dennis. *Adventure in Prolog*, Amzi!, 191 pgs, 2017.
- Blackburn, Patrick; Bos, Johan; Striegnitz, Kristina. *Learn Prolog Now!*, online, 2026.

---

## 6. Códigos fonte usados no resumo

| Arquivo | Conteúdo | Seção |
|---|---|---|
| [`fact.pl`](codes/fact.pl) | fatorial recursivo e de cauda | 2, 3 |
| [`fibo.pl`](codes/fibo.pl) | Fibonacci com `fib/1` | 3 |
| [`fibo2.pl`](codes/fibo2.pl) | Fibonacci de cauda (tempo) | 3 |
| [`fibo.py`](codes/fibo.py) | Fibonacci iterativo em Python (comparação) | 3 |
| [`car.pl`](codes/car.pl) | estrutura de dados (carro) | 4 |
| [`matrioskas.pl`](codes/matrioskas.pl) | estruturas + recursão | 4 |

**Fonte original:** `documento-base/Paradigmas/Programacao_Logica/05_recursao/codes/`.

---

### Como executar e testar (entradas e saídas verificadas)

```prolog
?- consult('codes/fact.pl').
true.
?- fact(5, F).
F = 120.
?- factorial(10, F).
F = 3628800.
?- factTR(6, 1, F).
F = 720.

?- consult('codes/fibo2.pl').     % contem diretiva de profile: a carga leva ~6,5 s
true.
?- fib(10, F).
F = 55.

?- consult('codes/car.pl').
true.
?- car(M, C, D).
M = peugeot, C = black, D = 2.
?- findall(M-C-D, car(M, C, D), L).
L = [peugeot-black-2,honda-red-4].

?- consult('codes/matrioskas.pl').
true.
?- dolls(a).
true.
?- list(a, 0).                    % imprime a arvore indentada com tab/4
-> a
    -> b
        -> d
...
```

> **Transcrição:** `fibo.pl` (não carrega; o que importa é
> `?- fib(200, F).` → `280571172992510140037611932413038677189525`). O `fibo2.pl` tem uma
> diretiva `?- profile(X is fib(1000000)).` que roda na carga e **demora**.

