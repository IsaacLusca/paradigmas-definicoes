# 02.5 - RecursÃ£o (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/05_recursao/` (`05_recursao.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**Data:** 27/08/2026
**CÃ³digos usados:** `codes/` (veja a seÃ§Ã£o 9)
**Aula:** 05 do mÃ³dulo ProgramaÃ§Ã£o LÃ³gica

---

## 1. Contexto / MotivaÃ§Ã£o

Prolog nÃ£o tem laÃ§os `for`/`while` como linguagens imperativas. Como escrever algoritmos que
repetem? A resposta Ã© a **recursÃ£o** â€” e em Prolog ela Ã© ainda mais natural, porque a
prÃ³pria unificaÃ§Ã£o e o backtracking conduzem a recursÃ£o. Nesta aula vocÃª vÃª como definir
predicados recursivos, a diferenÃ§a entre recursÃ£o **comum** e **de cauda**, e como usar
**estruturas de dados** (termos compostos) para modelar informaÃ§Ãµes.

---

## 2. Predicados recursivos

Um predicado Ã© definido **recursivamente** se uma ou mais regras de sua definiÃ§Ã£o se
referem **ao prÃ³prio predicado**. Toda definiÃ§Ã£o recursiva tem **duas partes**:

1. **ClÃ¡usulas-base** â€” fatos ou generalizaÃ§Ãµes (encerram a recursÃ£o).
2. **Regras recursivas** â€” chamam o prÃ³prio predicado (reduzem o problema).

O tratamento das regras recursivas se dÃ¡ **naturalmente pelo backtracking**.

```prolog
fact(N, F) :-
    N > 0,
    succ(NewN, N),
    fact(NewN, F1),
    F is F1*N.

fact(0, F) :- F is 1.
```

### CaracterÃ­sticas da recursÃ£o em Prolog

- **Cada nÃ­vel de recursÃ£o** tem seu prÃ³prio conjunto de variÃ¡veis.
- A **unificaÃ§Ã£o** entre o objetivo e o padrÃ£o de uma regra recursiva define as relaÃ§Ãµes
  entre as variÃ¡veis de diferentes nÃ­veis.
- Ã‰ preciso garantir que os **parÃ¢metros nÃ£o caracterizem uma clÃ¡usula-base**. Sem esse
  cuidado, um retorno pela porta `redo` em alguma subconsulta pode fazer um conjunto de
  valores que caracterizava uma clÃ¡usula-base **avanÃ§ar na regra recursiva**, causando
  erros como **laÃ§os infinitos**.

---

## 3. Exemplos de recursÃ£o

### Fibonacci (iterativo via acumuladores â€” recursÃ£o de cauda)

```prolog
fibTR(N, B, A, F) :-
    N > 0,
    succ(NewN, N),
    C is A + B,
    fibTR(NewN, C, B, F).

fibTR(0, _, Acc, Acc).

:- use_module(library(arithmetic)).
:- arithmetic_function(fib/1).

fib(N, F) :- fibTR(N, 1, 0, F).

?- X is fib(200).
X = 280571172992510140037611932413038677189525.
```

### RecursÃ£o simples Ã— recursÃ£o de cauda

Considere o fatorial. A versÃ£o **nÃ£o-cauda** (`fact/2`) faz o cÃ¡lculo **depois** da chamada
recursiva (`F is F1*N`), acumulando chamadas pendentes na pilha. A versÃ£o **de cauda**
(`factTR/3`) passa o resultado acumulado (`NewAcc`) para a prÃ³xima chamada:

```prolog
% recursÃ£o comum
fact(N, F) :- N > 0, succ(NewN, N), fact(NewN, F1), F is F1*N.
fact(0, F) :- F is 1.

% recursÃ£o de cauda (otimizada)
factTR(N, Acc, F) :-
    N > 0,
    succ(NewN, N),
    NewAcc is Acc * N,
    factTR(NewN, NewAcc, F).
factTR(0, Acc, F) :- F is Acc.

factorial(N, F) :- factTR(N, 1, F).
```

**DiferenÃ§a-chave:**

| RecursÃ£o comum | RecursÃ£o de cauda |
|---|---|
| O resultado depende da chamada recursiva (`F is F1*N`) | O resultado jÃ¡ vem acumulado na chamada |
| Acumula chamadas pendentes na pilha | Pode ser otimizada (a pilha nÃ£o cresce) |
| Sufoca a pilha em entradas grandes | Suporta entradas muito grandes |

> Comparando com Python (iterativo): o mesmo Fibonacci iterativo leva ~9,25s para $10^6$;
> em Prolog (recursÃ£o de cauda), ~6,49s.

---

## 4. Estruturas de dados em Prolog

Uma **estrutura de dados** combina termos primitivos (Ã¡tomos, inteiros) e estruturas em
**tipos compostos**. A sintaxe de declaraÃ§Ã£o Ã© idÃªntica a um fato:

```prolog
functor(arg1, arg2, ..., argN).
```

- Cada argumento pode ser um tipo primitivo ou **outra estrutura** (aninhamento).
- A declaraÃ§Ã£o de uma estrutura Ã© **semelhante Ã  declaraÃ§Ã£o de um fato**.

```prolog
car(peugeot, black, 2).
car(honda, red, 4).
```

### Consultas envolvendo estruturas

- A **ordem dos argumentos** influencia o resultado de uma consulta.

```prolog
car(X, red, _).        % que carro Ã© vermelho com 4 portas?
```

- Campos podem ser ignorados com a variÃ¡vel anÃ´nima `_`.
- Estruturas podem ser **aninhadas** para legibilidade:

```prolog
car(honda, color(red), doors(4)).
```

> As clÃ¡usulas `car(honda, red, 4)` e `car(honda, color(red), doors(4))` **nÃ£o unificam**:
> os argumentos sÃ£o estruturas diferentes (`red` â‰  `color(red)`).

### Exemplo â€” matrioskas (estrutura + recursÃ£o)

```prolog
matrioska(a, b).
matrioska(a, c).
matrioska(b, d).
matrioska(b, c).
matrioska(b, f).
matrioska(c, e).
matrioska(c, g).
matrioska(d, g).
matrioska(e, f).
matrioska(f, g).
matrioska(f, h).

% FunÃ§Ã£o recursiva auxiliar: imprime as matrioskas aninhadas
list_(M, L) :-
    nl,
    tab(L),
    print(->),
    tab(1),
    print(M),
    matrioska(M, X),
    NewL is L + 4,
    list_(X, NewL).

list(M, L) :- list_(M, L).
list(_, _).

doll(X) :- matrioska(X, _).
doll(X) :- matrioska(_, X).
```

---

## 5. ReferÃªncias

- SWI-Prolog: https://www.swi-prolog.org/
- WikipÃ©dia. *Prolog*, acesso em 13/03/2026.
- Merrit, Dennis. *Adventure in Prolog*, Amzi!, 191 pgs, 2017.
- Blackburn, Patrick; Bos, Johan; Striegnitz, Kristina. *Learn Prolog Now!*, online, 2026.

---

## 6. CÃ³digos fonte usados no resumo

| Arquivo | ConteÃºdo | SeÃ§Ã£o |
|---|---|---|
| [`fact.pl`](codes/fact.pl) | fatorial recursivo e de cauda | 2, 3 |
| [`fibo.pl`](codes/fibo.pl) | Fibonacci com `fib/1` | 3 |
| [`fibo2.pl`](codes/fibo2.pl) | Fibonacci de cauda (tempo) | 3 |
| [`fibo.py`](codes/fibo.py) | Fibonacci iterativo em Python (comparaÃ§Ã£o) | 3 |
| [`car.pl`](codes/car.pl) | estrutura de dados (carro) | 4 |
| [`matrioskas.pl`](codes/matrioskas.pl) | estruturas + recursÃ£o | 4 |

**Fonte original:** `documento-base/Paradigmas/Programacao_Logica/05_recursao/codes/`.

