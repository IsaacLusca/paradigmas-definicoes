# 02.3 - Backtracking (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/03_backtracking/` (`03_backtracking.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**Data:** 20/08/2026
**CÃ³digos usados:** `codes/` (veja a seÃ§Ã£o 9)
**Aula:** 03 do mÃ³dulo ProgramaÃ§Ã£o LÃ³gica

---

## 1. Contexto / MotivaÃ§Ã£o

Quando vocÃª pergunta algo ao Prolog e hÃ¡ **mais de uma resposta possÃ­vel**, o interpretador
retorna a primeira e para. O **backtracking** Ã© o mecanismo que permite "voltar" e continuar
a busca por outras respostas â€” Ã© o que torna a programaÃ§Ã£o lÃ³gica capaz de **enumerar**
soluÃ§Ãµes em vez de sÃ³ responder sim/nÃ£o. Sem ele, uma consulta teria no mÃ¡ximo uma resposta,
e programas como "liste todos os estudantes da UnB" nÃ£o funcionariam.

---

## 2. DefiniÃ§Ã£o

Quando Prolog tenta satisfazer um objetivo sobre um predicado:

1. Ele percorre **todas as clÃ¡usulas** que definem o predicado.
2. Se o objetivo e uma clÃ¡usula **unificam**, ela satisfaz o objetivo e Ã© **marcada**.
3. O interpretador retorna os valores **atados** (se hÃ¡ variÃ¡veis) ou apenas `true`
   (se todos os argumentos sÃ£o constantes).
4. Se o usuÃ¡rio solicitar com o **ponto-e-vÃ­rgula**, ele **retoma a busca** entre as
   clÃ¡usulas a partir da Ãºltima marcaÃ§Ã£o, **desatando** as variÃ¡veis previamente atadas.

Esse processo Ã© o **backtracking**.

---

## 3. Objetivos e fluxo de controle

Um objetivo tem **quatro portas** que representam o fluxo de controle de sua avaliaÃ§Ã£o:

| Porta | Momento |
|---|---|
| **call** | InÃ­cio da avaliaÃ§Ã£o do objetivo |
| **exit** | Alguma clÃ¡usula unificou â€” termina, atando as variÃ¡veis |
| **fail** | Nenhuma clÃ¡usula unificou â€” a avaliaÃ§Ã£o falha |
| **redo** | ApÃ³s sucesso, o usuÃ¡rio digitou `;` â€” retoma do ponto parado, desatando variÃ¡veis |

```
            call â”€â”€â–º â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â” â”€â”€â–º exit
                      â”‚ Objetivo â”‚
            fail â—„â”€â”€ â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜ â—„â”€â”€ redo
```

O fluxo **entra** por `call` e **sai** por `exit` (sucesso) ou `fail` (falha). Ao pedir mais
respostas, reentra-se por `redo`.

---

## 4. Consultas compostas

Consultas simples podem ser combinadas em **consultas compostas** com o operador `,/2`
(**e** lÃ³gico):

```prolog
?- query1(a1, ..., ak), query2(b1, ..., br), ..., queryN(z1, ..., zs).
```

- Se a **mesma variÃ¡vel** aparece em mais de um predicado, ela deve assumir o **mesmo valor**
  em todos.
- O **escopo** de uma variÃ¡vel lÃ³gica Ã© a consulta (simples ou composta).

```prolog
?- cidade(X, mt), capital(X).
?- cidade(X, Y), regiao(centro_oeste, Y), capital(X).
```

### Fluxo de controle de consultas compostas

- O backtracking tenta unificar os objetivos **da esquerda para a direita**.
- Termina com **sucesso** apenas se sai pela porta `exit` do predicado **mais Ã  direita**.
- Se uma consulta sai por `redo`, apenas as variÃ¡veis **atadas no predicado da porta** sÃ£o
  desatadas (as dos predicados Ã  esquerda permanecem atadas).
- Se a consulta **falha** para algum predicado, ela falha **como um todo**.
- O `;` forÃ§a uma reentrada no **Ãºltimo** predicado pela porta `redo`.

---

## 5. Predicados extra-lÃ³gicos

SÃ£o **predicados prÃ©-definidos** em Prolog, para os quais **nÃ£o hÃ¡ clÃ¡usulas**. Quando um
objetivo unifica com um deles, o interpretador chama uma **rotina interna** que executa
tarefas **fora do contexto da prova de teoremas** (ex.: escrever no terminal).

Eles respondem tanto na porta `call` quanto na `redo`. A resposta na porta `redo` Ã© chamada
**comportamento no backtracking**.

| Predicado | Resposta na `call` | Resposta na `redo` |
|---|---|---|
| `write/1` | Casa com qualquer padrÃ£o; como efeito colateral, escreve o argumento no console | **Sempre falha** |
| `nl/0` | Sempre bem-sucedido; inicia uma nova linha | Sempre falha |
| `tab/1` | AvanÃ§a o cursor N espaÃ§os | Sempre falha |
| `fail/0` | Sempre falha (equivale a `false/0`) | â€” |

> O **comportamento no `redo`** de `write/1` (sempre falha) Ã© o que permite gerar uma saÃ­da
> repetida com forÃ§a bruta. Ex.: o predicado abaixo imprime **todos** os quadrilÃ¡teros que
> tÃªm lados iguais e Ã¢ngulos retos, sem interaÃ§Ã£o:

```prolog
?- quadrilateral(X), equal_sides(X), write(X), nl, fail.
```

O `fail/0` forÃ§a o backtracking a continuar, gerando cada soluÃ§Ã£o; a clÃ¡usula final `unb_report.`
evita que o resultado geral seja falso.

---

## 6. Condicionais

O predicado extra-lÃ³gico `->/2` habilita condicionais em Prolog. Sintaxes:

```prolog
If -> Then
If -> Then ; Else
```

- Se `If` corresponder a um predicado **verdadeiro**, o fluxo segue para `Then`.
- No segundo caso, se `If` for **falso**, o fluxo segue para `Else`.

Exemplo â€” gerar a **tabela-verdade** de um conectivo binÃ¡rio:

```prolog
:- meta_predicate execute_goal(2).

execute_goal(Op, X, Y) :-
    (call(Op, X, Y) -> writeln(true) ; writeln(false)).

truth_table(Op, Symbol) :-
    bool(X), bool(Y),
    write(X), write(' '), write(Symbol), write(' '), write(Y), write(' = '),
    execute_goal(Op, X, Y), fail.
truth_table(_, _).

?- truth_table(and, '&').
true & true = true
true & false = false
false & true = false
false & false = false
true.
```

---

## 7. Rastreamento (trace)

- `trace/0` permite rastrear o fluxo de controle â€” todas as consultas subsequentes sÃ£o
  rastreadas.
- `creep` avanÃ§a para o prÃ³ximo passo da execuÃ§Ã£o (no SWI-Prolog, pela barra de espaÃ§o).
- `notrace/0` desativa o rastreamento; em seguida, consulte `nodebug/0`.

---

## 8. ReferÃªncias

- SWI-Prolog: https://www.swi-prolog.org/
- WikipÃ©dia. *Prolog*, acesso em 10/11/2020.
- Merrit, Dennis. *Adventure in Prolog*, Amzi!, 191 pgs, 2017.

---

## 9. CÃ³digos fonte usados no resumo

| Arquivo | ConteÃºdo | SeÃ§Ã£o |
|---|---|---|
| [`capital.pl`](codes/capital.pl) | consultas compostas | 4 |
| [`unb_report.pl`](codes/unb_report.pl) | `write`/`nl`/`tab`/`fail` | 5 |
| [`quadrilaterals.pl`](codes/quadrilaterals.pl) | `fail` para listar todos | 5 |
| [`truth_table.pl`](codes/truth_table.pl) | condicionais + `call/N` | 6 |

**Fonte original:** `documento-base/Paradigmas/Programacao_Logica/03_backtracking/codes/`.

