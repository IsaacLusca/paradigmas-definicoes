# 02.4 - AritmÃ©tica (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/04_aritmetica/` (`04_aritmetica.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**Data:** 25/08/2026
**CÃ³digos usados:** `codes/` (veja a seÃ§Ã£o 9)
**Aula:** 04 do mÃ³dulo ProgramaÃ§Ã£o LÃ³gica

---

## 1. Contexto / MotivaÃ§Ã£o

AtÃ© aqui, Prolog sÃ³ **prova** coisas (verdadeiro/falso) â€” ele nÃ£o calcula. Mas a maioria dos
programas precisa fazer **contas**. O problema: como um paradigma lÃ³gico, baseado em
unificaÃ§Ã£o, faz aritmÃ©tica? A resposta Ã© o operador `is/2`, que **avalia** uma expressÃ£o
aritmÃ©tica e **ata** o resultado a uma variÃ¡vel. Esta aula apresenta os operadores, a
precedÃªncia, as funÃ§Ãµes aritmÃ©ticas e como lidar com predicados que tÃªm **mais de uma
semÃ¢ntica** (funcionam "nas duas direÃ§Ãµes").

---

## 2. Predicado `is/2`

O predicado extra-lÃ³gico `is/2` Ã© usado para **computar expressÃµes aritmÃ©ticas**:

```prolog
Number is Expr.
```

- O resultado da expressÃ£o `Expr` Ã© **atado** Ã  variÃ¡vel `Number`.
- **ParÃªnteses** podem ser usados para evitar ambiguidades e alterar a ordem de precedÃªncia.

```prolog
?- X is 1 + 2 * 3.
X = 7.

?- X is (1 + 2) * 3.
X = 9.

?- X is 2 / 5.
X = 0.4.

?- X is 2 - 5.
X = -3.

?- X is 2 ^ 5.
X = 32.

?- X is 20 // 3.
X = 6.
```

---

## 3. Operadores

**Operadores** sÃ£o predicados que suportam, alÃ©m da prÃ©-fixada padrÃ£o, as notaÃ§Ãµes
**infixada** ou **pÃ³s-fixada**. Novos operadores sÃ£o declarados com a diretiva `op/3`:

```prolog
:- op(Precedence, Type, Name).
```

- `Name` = Ã¡tomo que nomeia o operador; `Precedence` = inteiro entre 0 e 1200 â€”
  **quanto menor o valor, maior a precedÃªncia**.
- `Type` = Ã¡tomo com os caracteres `x`, `y` e `f`, que determina a **posiÃ§Ã£o** do operador
  (indicada por `f`) e a **associatividade** (posiÃ§Ã£o de `y`).
  - `xfy` â†’ binÃ¡rio, infixado, **associativo Ã  direita**.
  - `xf` â†’ unÃ¡rio, **pÃ³s-fixado**.
  - `yfx` â†’ associativo Ã  esquerda (aritmÃ©tica usual).
- PrecedÃªncia `0` **apaga** a declaraÃ§Ã£o do operador.
- O tipo/precedÃªncia de um operador prÃ©-definido pode ser consultado com `current_op/3`:

```prolog
current_op(Precedence, Type, Name).
```

> Detalhe jÃ¡ visto na aula 01: `,` Ã© `1000 xfy`, `;` Ã© `1100 xfy`, `\+` Ã© `900 fy`.

---

## 4. Operadores aritmÃ©ticos e relacionais

### AritmÃ©ticos

| Operador | Tipo | PrecedÃªncia | DescriÃ§Ã£o |
|---|---|---|---|
| `+/2` | yfx | 500 | AdiÃ§Ã£o |
| `-/2` | yfx | 500 | SubtraÃ§Ã£o |
| `*/2` | yfx | 400 | MultiplicaÃ§Ã£o |
| `//2` | yfx | 400 | DivisÃ£o nos reais |
| `div/2` | yfx | 400 | Quociente da divisÃ£o inteira |
| `mod/2` | yfx | 400 | Resto euclidiano da divisÃ£o inteira |
| `^/2` | xfy | 200 | PotÃªncia (`a ^ b` = $a^b$) |

Exemplo de regra com expressÃµes (convenÃ§Ã£o: **o valor de retorno Ã© o Ãºltimo parÃ¢metro**):

```prolog
number_of_lines(N, Res) :- Res is 2^N.

juros_simples(M, J, T, Res) :- Res is M * (1 + J * T).
juros_compostos(M, J, T, Res) :- Res is M * (1 + J) ** T.

?- number_of_lines(3, N).
N = 8.

?- juros_simples(100, 0.12, 24, Res).
Res = 388.0.
```

### Relacionais

| Operador | Tipo | PrecedÃªncia | DescriÃ§Ã£o |
|---|---|---|---|
| `>/2` | xfx | 700 | Maior que |
| `</2` | xfx | 700 | Menor que |
| `>=/2` | xfx | 700 | Maior ou igual |
| `=</2` | xfx | 700 | Menor ou igual |
| `=:=/2` | xfx | 700 | Igual (valor) |
| `=\=/2` | xfx | 700 | Diferente (valor) |

Exemplo â€” raÃ­zes reais de $ax^2+bx+c$:

```prolog
roots(A, B, C, X) :-
    A =\= 0,
    Delta is B^2 - 4*A*C,
    S is root_signal(Delta),
    X is (-B + S*sqrt(Delta))/(2*A).
```

---

## 5. FunÃ§Ãµes aritmÃ©ticas

Os operadores aritmÃ©ticos sÃ£o chamados **funÃ§Ãµes aritmÃ©ticas**, pois **"retornam"** o valor
de suas operaÃ§Ãµes (em vez de apenas atÃ¡-los a uma variÃ¡vel). Para tornar um **predicado
prÃ³prio** em funÃ§Ã£o aritmÃ©tica:

1. Carregar a biblioteca de aritmÃ©tica: `:- use_module(library(arithmetic)).`
2. Declarar o predicado como funÃ§Ã£o aritmÃ©tica com a diretiva `arithmetic_function`.
3. A variÃ¡vel a ser "retornada" deve ser a **Ãºltima** dos argumentos, e a aridade declarada
   Ã© uma **unidade menor** que a aridade do predicado.

```prolog
:- use_module(library(arithmetic)).
:- arithmetic_function(number_of_lines/1).

number_of_lines(N, Res) :- Res is 2^N.

?- X is 1 + number_of_lines(4).
X = 17.
```

---

## 6. Predicados com mÃºltiplas semÃ¢nticas

Alguns predicados podem ter **mais de uma semÃ¢ntica**. Ex.: `succ/2` serve para **sucessor**
e **antecessor**:

```prolog
?- succ(2, S).
S = 3.

?- succ(A, 2).
A = 1.
```

Ã‰ possÃ­vel implementÃ¡-los com **condicionais** e predicados extra-lÃ³gicos que verificam o
tipo de um termo:

- `var/1` â€” verdadeiro se a variÃ¡vel Ã© **livre** (ainda nÃ£o atada).
- `nonvar/1` â€” verdadeiro se a variÃ¡vel jÃ¡ foi **atada**.
- `integer/1`, `number/1`, `atom/1` â€” confirmam o **tipo** do termo.

Exemplo â€” conversÃ£o Celsius/Fahrenheit em ambas as direÃ§Ãµes:

```prolog
celsius_fahrenheit(C, F) :-
    ((var(C), var(F))
        -> instantiation_error('Ao menos uma variÃ¡vel deve estar atada') ; true),
    (nonvar(C) -> F is C*9/5 + 32 ; C is (F - 32)*5/9).

?- celsius_fahrenheit(40, F).
F = 104.

?- celsius_fahrenheit(C, 104).
C = 40.

?- celsius_fahrenheit(100, 100).
false.
```

Outro exemplo â€” resolver um **Sudoku 4x4** com permutaÃ§Ãµes e comparaÃ§Ãµes:

```prolog
permutation(A, B, C, D) :-
    between(1, 4, A), between(1, 4, B),
    between(1, 4, C), between(1, 4, D),
    A =\= B, A =\= C, A =\= D,
    B =\= C, B =\= D, C =\= D.
```

---

## 7. ReferÃªncias

- SWI-Prolog: https://www.swi-prolog.org/
- WikipÃ©dia. *Prolog*, acesso em 13/03/2026.
- Merrit, Dennis. *Adventure in Prolog*, Amzi!, 191 pgs, 2017.

---

## 8. CÃ³digos fonte usados no resumo

| Arquivo | ConteÃºdo | SeÃ§Ã£o |
|---|---|---|
| [`is.pl`](codes/is.pl) | `is/2` e expressÃµes | 2 |
| [`expr.pl`](codes/expr.pl) | regras com expressÃµes | 4 |
| [`user_arithmetic_function.pl`](codes/user_arithmetic_function.pl) | `arithmetic_function` | 5 |
| [`roots.pl`](codes/roots.pl) | operadores relacionais + funÃ§Ã£o aritmÃ©tica | 4, 5 |
| [`succ1.pl`](codes/succ1.pl) | `succ/2` (sucessor) | 6 |
| [`succ2.pl`](codes/succ2.pl) | `succ/2` (antecessor) | 6 |
| [`celsius.pl`](codes/celsius.pl) | mÃºltiplas semÃ¢nticas + `var`/`nonvar` | 6 |
| [`sudoku.pl`](codes/sudoku.pl) | Sudoku 4x4 | 6 |

**Fonte original:** `documento-base/Paradigmas/Programacao_Logica/04_aritmetica/codes/`.

