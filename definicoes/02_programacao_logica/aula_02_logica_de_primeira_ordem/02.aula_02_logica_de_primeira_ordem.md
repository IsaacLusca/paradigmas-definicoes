# 02.2 - LÃ³gica de Primeira Ordem (Prolog)

**Fonte:** `documento-base/Paradigmas/Programacao_Logica/02_logica_de_primeira_ordem/` (`02_logica_de_primeira_ordem.pdf`, `main.tex`, `script.md`)
**Linguagem:** Prolog (SWI-Prolog)
**Data:** 18/08/2026
**CÃ³digos usados:** `codes/` (veja a seÃ§Ã£o 9)
**Aula:** 02 do mÃ³dulo ProgramaÃ§Ã£o LÃ³gica

---

## 1. Contexto / MotivaÃ§Ã£o

A lÃ³gica proposicional (aula 01) trata de proposiÃ§Ãµes **atÃ´micas** â€” frases completas que sÃ£o
verdadeiras ou falsas. Mas a matemÃ¡tica e a programaÃ§Ã£o precisam de algo mais: expressar
**generalizaÃ§Ãµes** como "todo estudante da UnB..." ou "existe um X tal que...". Para isso,
a lÃ³gica de primeira ordem introduz **variÃ¡veis**, **quantificadores** e **sentenÃ§as abertas**
(funÃ§Ãµes proposicionais). Em Prolog, essas ideias viram **regras** e **consultas** â€” o
vocabulÃ¡rio que permite escrever programas de verdade.

---

## 2. VariÃ¡veis e constantes

- Uma **variÃ¡vel** Ã© um sÃ­mbolo que representa um objeto **nÃ£o especificado** de um
  determinado conjunto $U$, chamado **conjunto universal** da variÃ¡vel.
- Uma **constante** Ã© um **membro especÃ­fico** do conjunto universal.

> Ex.: em `paciente('Carlos', 38)`, `'Carlos'` e `38` sÃ£o **constantes**. JÃ¡ uma consulta
> `paciente(X, Idade)` tem `X` e `Idade` como **variÃ¡veis** (objetos a descobrir).

---

## 3. Termos em Prolog

Prolog tem um **Ãºnico tipo de dado**, chamado **termo**. Todos os argumentos de predicados
sÃ£o termos. Os termos podem ser:

- **Ãtomos** â€” nomes de propÃ³sito geral, iniciados com **letra minÃºscula** (ex.: `dolar`,
  `eixo_x`). Para usar maiÃºsculas ou espaÃ§os, delimite com **aspas simples**: `'Pedro Lucas'`.
- **NÃºmeros** (inteiros ou flutuantes) â€” termos primitivos.
- **VariÃ¡veis** â€” subtipo de termo, iniciadas com **maiÃºscula** ou `_`.

```prolog
% Valores em 10/03/2026
cotacao(dolar, 5.16).
cotacao(euro, 6.01).
cotacao(iene, 0.03).

% O primeiro argumento Ã© o nome, o segundo a idade
paciente('Carlos', 38).
paciente('Maria', 26).
paciente('Pedro Lucas', 40).

% Ãtomos podem conter underscore e nÃºmeros
limites(eixo_x, -10, 10).
```

---

## 4. SentenÃ§as abertas

Uma **sentenÃ§a aberta** $S(x)$ em $x$ Ã© uma expressÃ£o em que o sÃ­mbolo $x$ ocorre uma ou
mais vezes e que, **se todas as ocorrÃªncias de $x$ forem substituÃ­das por uma constante**
$v$ do conjunto universal de $x$, $S(v)$ torna-se uma **proposiÃ§Ã£o** (passÃ­vel de V/F).
SentenÃ§as abertas tambÃ©m sÃ£o chamadas **predicados** ou **funÃ§Ãµes proposicionais**.

> Ex.: $S(x)$ = "x Ã© estudante da FCTE". Substituindo `x = ana`, temos a proposiÃ§Ã£o
> "ana Ã© estudante da FCTE" â€” verdadeira. Com `x = diana`, falsa.

---

## 5. VariÃ¡veis e regras em Prolog

As variÃ¡veis permitem declarar **sentenÃ§as abertas** em Prolog, chamadas **regras**.
Sintaxe da regra `rule/N`:

```prolog
rule(X1, X2, ..., XN) :- body.
```

| Parte | Significado |
|---|---|
| `rule` | **nome** do predicado |
| `X1..XN` | **argumentos** |
| `N` | **aridade** |
| `body` | **corpo** (proposiÃ§Ã£o composta) |
| `.` | **terminador** |

### Declarando regras

- Regras devem estar em arquivo `.pl`.
- O corpo deve ser uma **proposiÃ§Ã£o composta** ou ser **omitido**.
- **Regras sem corpo = fatos** (sÃ£o equivalentes):

```prolog
f(1).
f(1) :- true.
```

- Podem existir **mÃºltiplas regras** para o mesmo predicado â€” o predicado Ã© a **disjunÃ§Ã£o**
  (ou) de todas as regras e fatos declarados:

```prolog
fcte(ana).
fcte(beto).
fcte(carlos).
darcy(diana).
darcy(esther).
ifb(fernando).

unb(X) :- (fcte(X) ; darcy(X)).

unb2(X) :- fcte(X).
unb2(X) :- darcy(X).
```

- Em Prolog, **fatos e regras sÃ£o denominados clÃ¡usulas**.

---

## 6. Quantificador existencial

Seja $S(x)$ uma sentenÃ§a aberta. O **quantificador existencial** $\exists$ Ã© usado em
$\exists x.S(x)$, que significa: **existe pelo menos um** elemento $v$ no conjunto universal
de $x$ tal que $S(v)$ Ã© verdadeira.

Em Prolog, o quantificador existencial estÃ¡ associado Ã s **consultas**:

- Uma **consulta simples** Ã© uma sentenÃ§a aberta simples (predicado + argumentos).
- Se um ou mais argumentos sÃ£o substituÃ­dos por uma **variÃ¡vel lÃ³gica** e a consulta for
  verdadeira, o interpretador indica as **atribuiÃ§Ãµes** que a tornaram verdadeira.
- Se houver **duas ou mais** atribuiÃ§Ãµes, digite `;` (ponto-e-vÃ­rgula) no interpretador
  para obter as demais.

```prolog
?- unb(fernando).
false.

?- unb(diana).
true.

?- ifb(X).
X = fernando.

?- unb(X).
X = ana ;
X = beto ;
X = carlos ;
X = diana ;
X = esther.
```

### Consultas e variÃ¡veis lÃ³gicas

- As consultas sÃ£o avaliadas por **unificaÃ§Ã£o** (casamento de padrÃµes).
- O padrÃ£o de uma consulta Ã© chamado **objetivo** (*goal*).
- **VariÃ¡veis lÃ³gicas nÃ£o se comportam como as de outras linguagens**: durante a
  unificaÃ§Ã£o, uma variÃ¡vel **casa com qualquer termo**; apÃ³s unificar, a variÃ¡vel fica
  **atada** (*to bind*) ao termo que casou.

---

## 7. UnificaÃ§Ã£o (versÃ£o simplificada)

Se a base Prolog contÃ©m apenas **fatos**, a unificaÃ§Ã£o Ã© bem-sucedida se as trÃªs condiÃ§Ãµes
abaixo forem satisfeitas:

1. O **predicado** do objetivo e da base Ã© o **mesmo**;
2. Ambos tÃªm a **mesma aridade**;
3. **Todos os argumentos sÃ£o os mesmos**.

```prolog
f(1, 2).
f(1, 3).
f(2, 3).
g(1).
g(2).

?- f(1, 2).
true.

?- f(2, 1).
false.

?- f(1).
false.        % aridade diferente

?- g(1, 2).
false.
```

**Mesma variÃ¡vel lÃ³gica** em dois argumentos forÃ§a que eles tenham o mesmo valor:

```prolog
f(1, 1). f(1, 2). f(1, 3).
f(2, 1). f(2, 2). f(2, 3).
g(a, 1). g(1, a).

?- f(X, X).
X = 1 ; X = 2 ;
false.

?- g(X, X).
false.
```

---

## 8. Fatos dinÃ¢micos

Prolog permite **declarar ou retratar fatos diretamente no interpretador**:

- O predicado deve ser declarado **dinÃ¢mico** com a diretiva `dynamic` (nome + aridade),
  **sÃ³ em arquivo**.
- `asserta/1` adiciona um fato no **inÃ­cio** da base; `assertz/1` adiciona no **fim**.
- `retract/1` **apaga** um fato da base.

```prolog
:- dynamic
    coordenador/1.

coordenador(matheus).       % CNA
coordenador(himilsys).      % Automotiva
coordenador(batistuta).     % EletrÃ´nica
coordenador(sebastien).     % Aeroespacial

?- asserta(coordenador(andrea)).   % Energia
true.

?- assertz(coordenador(john)).     % Software
true.

?- retract(coordenador(matheus)).
true.

?- coordenador(X).
X = andrea ;
X = himilsys ;
X = batistuta ;
X = sebastien ;
X = john.
```

---

## 9. Quantificador universal e generalizaÃ§Ãµes

O **quantificador universal** $\forall x.S(x)$ significa que **para todos** os valores $v$
no conjunto universal de $x$, $S(v)$ Ã© verdadeira.

Em Prolog, o $\forall$ Ã© expresso por uma **generalizaÃ§Ã£o** â€” um **fato que casa com
qualquer valor**:

```prolog
is_term(_).
```

- `is_term(_)` Ã© verdadeiro para qualquer argumento (um argumento de predicado sempre Ã©
  um termo).
- Como a variÃ¡vel nÃ£o Ã© usada no corpo, usa-se o **wildcard** `_` em vez de uma variÃ¡vel
  nomeada.

```prolog
?- is_term(1).     % NÃºmeros sÃ£o termos
true.
?- is_term(abc).   % Ãtomos sÃ£o termos
true.
?- is_term(A).     % VariÃ¡veis sÃ£o termos
true.
?- is_term(A, B).  % erro: is_term tem aridade 1
```

---

## 10. CÃ³digos fonte usados no resumo

| Arquivo | ConteÃºdo | SeÃ§Ã£o |
|---|---|---|
| [`terms.pl`](codes/terms.pl) | Ã¡tomos, nÃºmeros, constantes | 3 |
| [`rules.pl`](codes/rules.pl) | fatos e regras (disjunÃ§Ã£o) | 5 |
| [`equiv.pl`](codes/equiv.pl) | fato â‰¡ regra sem corpo | 5 |
| [`consult.pl`](codes/consult.pl) | consultas simples + variÃ¡veis | 6 |
| [`unification.pl`](codes/unification.pl) | unificaÃ§Ã£o simplificada | 7 |
| [`variables.pl`](codes/variables.pl) | unificaÃ§Ã£o + variÃ¡veis | 7 |
| [`same_var.pl`](codes/same_var.pl) | mesma variÃ¡vel em args | 7 |
| [`dynamic.pl`](codes/dynamic.pl) | fatos dinÃ¢micos | 8 |
| [`is_term.pl`](codes/is_term.pl) | generalizaÃ§Ã£o com `_` | 9 |

**Fonte original:** `documento-base/Paradigmas/Programacao_Logica/02_logica_de_primeira_ordem/codes/`.

